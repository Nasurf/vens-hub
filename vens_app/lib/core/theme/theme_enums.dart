// Defines the available theme modes for the application.
enum AppThemeMode { light, dark, system }

// Defines the available color schemes for the application.
enum AppColorScheme { teal, purple, orange, green, blue, pink, greyscale }
